- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.gitconfig:
      path: gitconfig
      force: true

- shell:
    - [git submodule update --init --recursive, Installing submodules]
