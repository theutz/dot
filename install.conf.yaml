- defaults:
    link:
      relink: true
      force: true

- clean: ["~"]

- link:
    ~/.gitconfig:
      path: gitconfig
    ~/.tmux.conf:
      path: tmux.conf

- shell:
    - [git submodule update --init --recursive, Installing submodules]
