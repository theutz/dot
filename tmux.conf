#######################
# Pre-Plugin Settings #
#######################

# Setup prefix
unbind C-b
set -g prefix C-a
# bind-key -r C-s send-prefix

# Tmux  Variables
# set -g default-terminal "xterm-256color"
# set -ga terminal-overrides ",xterm-256color:Tc"

# set -g @continuum-restore 'on'
# set -g @continuum-save-interval 5
# set -g @resurrect-strategy-vim 'session'
# set -g @resurrect-strategy-nvim 'session'
# set -g @themepack-status-right-area-left-format "#h"
# set -g @theme-status-left-length 80
# set -g @theme-status-right-length 80
# set -g @dracula-show-powerline true
# set -g @dracula-show-left-icon session
# set -g @srcery_tmux_patched_font '1'
set -g @catppuccin_flavour 'macchiato'

###########
# PLUGINS #
###########

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
# set-option -g @plugin 'b0o/tmux-autoreload'
# set -g @plugin 'tmux-plugins/tmux-sessionist'
# set -g @plugin 'tmux-plugins/tmux-pain-control'
# set -g @plugin 'christoomey/vim-tmux-navigator'
# set -g @plugin 'sainnhe/tmux-fzf'
# set -g @plugin 'tmux-plugins/tmux-resurrect'
# set -g @plugin 'tmux-plugins/tmux-continuum'

### Theme ###
# set -g @plugin 'jimeh/tmux-themepack'
#set -g @plugin 'arcticicestudio/nord-tmux'
# set -g @plugin 'dracula/tmux'
# set -g @plugin 'srcery-colors/srcery-tmux'
set -g @plugin 'rwxd/catppuccin-tmux'

########################
# Post-Plugin Settings #
########################

setw -g mouse on
set -g base-index 1
setw -g pane-base-index 1
set -g mode-keys vi
set -g renumber-windows on

# Bindings
# bind-key -N "Respawn the current pane" C-x confirm -p "Respawn pane? (y/n)" 'respawn-pane -k -c "#{pane_current_path}"'
# bind-key -N "Clear the current screen" C-l send-keys 'C-l'
# bind-key -N "Open a popup window with a shell" ` display-popup -E -h 80% -w 80%
# bind-key -N "Open a popup window with btop" b display-popup -E -h 80% -w 80% btop
# bind-key -N "Open a popup window with yadm in lazygit" y display-popup -E -h 80% -w 80% '(cd $HOME && yadm enter lazygit)'
#
# set-option -s set-clipboard off
# bind P paste-buffer
# bind-key -T copy-mode-vi v send-keys -X begin-selection
# bind-key -T copy-mode-vi y send-keys -X rectangle-toggle
# unbind -T copy-mode-vi Enter
# bind-key -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel 'pbcopy'
# bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel 'pbcopy'

if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"
run '~/.tmux/plugins/tpm/tpm'
